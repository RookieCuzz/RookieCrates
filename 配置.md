# RookieCrates 配置流程

RookieCrates 当前采用“管理 GUI 配置 + SQLite 保存”的方式。宝箱、奖池、场景点、模型入口和玩家数据均以 SQLite 为准；`config.yml` 只保存数据库路径、演出时序、广播格式、GUI 标题和消息文本等全局设置。

## 1. 安装前置

1. 推荐将 `target/RookieCrates-1.0-SNAPSHOT-server-package.zip` 解压到服务器根目录；它已包含插件本体、默认配置、模型和文档。手动安装时，将 `RookieCrates-1.0-SNAPSHOT.jar` 放入服务器的 `plugins` 目录。
2. 安装完整的 ModelEngine R4。ModelEngine 是硬依赖，缺失时 RookieCrates 无法启动。
3. 如果宝箱金币价格大于 `0`，还需安装 Vault 和可用的经济插件。
4. 将 `model-assets/ModelEngine/blueprints` 中的五个模型复制到服务器的 `plugins/ModelEngine/blueprints`：

   - `default_crate.bbmodel`
   - `loot_white.bbmodel`
   - `loot_pink.bbmodel`
   - `loot_blue.bbmodel`
   - `loot_yellow.bbmodel`

5. 完整重启服务器，让 ModelEngine 构建模型缓存和资源包。

默认使用的模型与动画为：

```text
宝箱模型：default_crate
奖励模型：loot_white
待机动画：idle
开箱动画：open2
```

Loot 模型必须包含 `item` 骨骼；`tag_name` 骨骼为可选项。

## 2. 创建宝箱

管理员在游戏内执行：

```text
/rc admin
```

点击“创建宝箱”，依次输入：

1. 宝箱 ID，例如 `default`。只允许小写字母、数字、下划线和连字符，最多 32 位。
2. 宝箱显示名称。

新建宝箱的默认设置来自 `config.yml` 的 `defaults.crate`：

| 配置项 | 默认值 |
| --- | --- |
| 状态 | 禁用 |
| 单抽价格 | `0` |
| 七连价格 | `0` |
| A 级保底 | `10` |
| S 级保底 | `80` |
| 宝箱模型 | `default_crate` |
| 奖励模型 | `loot_white` |
| 待机动画 | `idle` |
| 开箱动画 | `open2` |
| 交互碰撞箱 | `1.5 × 2.0` |
| 允许跳过 | 是 |
| S 级广播 | 是 |

宝箱创建后保持禁用，建议完成所有配置并测试后再启用。

## 3. 配置钥匙、价格、保底和模型

进入对应宝箱的管理页面：

- 钥匙模板：主手拿着要作为钥匙的物品，然后点击“钥匙模板”。
- 价格：左键设置单抽价格，右键设置七连套餐价格。
- 保底：左键设置 A 级保底，右键设置 S 级保底，必须满足 `0 < A <= S`。
- 模型与动画：点击后在聊天栏按以下格式输入四个非空值：

  ```text
  default_crate|loot_white|idle|open2
  ```

- 交互碰撞箱：输入宽度和高度，例如：

  ```text
  1.5 2.0
  ```

- 允许跳过：点击切换。
- S 级广播：点击切换。

聊天输入过程中可使用 `/cancel` 取消，输入会在 60 秒后超时。

## 4. 配置奖池

在宝箱管理页面点击“添加奖励”。

### 物品奖励

主手拿着要发给玩家的真实物品，然后点击添加奖励。依次输入奖励 ID 和显示名称，插件会保存该物品及其数量。

### 命令奖励

保持主手为空后点击添加奖励，再输入奖励 ID、显示名称和控制台命令。多条命令使用 `;` 分隔，命令中的 `%player%` 会替换为获奖玩家名称。

奖励可以同时包含物品和控制台命令，但至少要保留其中一种发放内容。

每个奖励可以继续编辑：

- 显示名称
- 正数权重
- 稀有度
- 真实奖励物品
- 控制台命令
- 独立广播开关

新奖励默认权重为 `1.0`、稀有度为 `E`、广播关闭。权重代表相对概率；奖励 ID 当前在整个数据库中全局唯一，不同宝箱之间也不要重复。

## 5. 配置演出场景

进入宝箱管理页面的“场景点”，需要保存完整的九个点：

```text
CRATE
CAMERA
LOOT_1
LOOT_2
LOOT_3
LOOT_4
LOOT_5
LOOT_6
LOOT_7
```

站到目标位置、调整好朝向后点击对应场景点。插件会保存当前世界、坐标、Yaw 和 Pitch。

各点用途：

- `CRATE`：开箱演出中的宝箱位置。
- `CAMERA`：开箱演出时玩家的镜头位置和朝向。
- `LOOT_1`～`LOOT_7`：七连抽的七个奖励展示位置；单抽使用其中的展示位置。

也可以使用命令设置：

```text
/rc scene <宝箱ID> CRATE
/rc scene <宝箱ID> CAMERA
/rc scene <宝箱ID> LOOT_1
/rc scene <宝箱ID> LOOT_2
/rc scene <宝箱ID> LOOT_3
/rc scene <宝箱ID> LOOT_4
/rc scene <宝箱ID> LOOT_5
/rc scene <宝箱ID> LOOT_6
/rc scene <宝箱ID> LOOT_7
```

## 6. 放置玩家右键入口

站到大厅中需要显示公共宝箱模型的位置，在管理 GUI 中左键点击“模型放置”，或者执行：

```text
/rc place <宝箱ID>
```

该位置是玩家右键的正式入口，与演出场景中的 `CRATE` 点是两个独立配置，可以相同，也可以不同。

移除入口：

```text
/rc remove <宝箱ID>
```

也可以在管理 GUI 中右键“模型放置”。

只有以下内容全部有效时，公共宝箱模型和交互实体才会生成：

- ModelEngine 已正常加载对应模型。
- 宝箱模型存在配置的待机动画。
- 已配置模型入口位置。
- 已配置 `CRATE` 和 `CAMERA`。
- 已配置 `LOOT_1`～`LOOT_7`。

## 7. 启用宝箱

完成以下检查后，在管理 GUI 中点击状态按钮启用宝箱：

- 已设置钥匙模板。
- 至少有一个有效奖励。
- 所有奖励权重大于 `0`，并包含物品或命令。
- 九个场景点完整。
- 模型入口已经放置并正常显示。
- 模型 ID、动画 ID 和 `item` 骨骼有效。
- 付费宝箱的 Vault 经济服务可用。

## 8. 发放钥匙

发放实体钥匙：

```text
/rc key give <玩家> <宝箱ID> <数量> physical
```

发放虚拟钥匙：

```text
/rc key give <玩家> <宝箱ID> <数量> virtual
```

实体钥匙会写入宝箱身份标记；玩家背包放不下时，多出的实体钥匙会掉落在玩家脚下。即使只使用虚拟钥匙，宝箱本身仍必须设置钥匙模板。

## 9. 玩家使用流程

玩家可以右键大厅中的公共宝箱模型进入 GUI，也可以使用：

```text
/rc
/rc open <宝箱ID>
```

玩家选择：

- 单抽：消耗 1 把钥匙和单抽价格。
- 七连抽：消耗 7 把钥匙和七连套餐价格。

抽取结果会先持久化到 SQLite，再播放演出。演出结束后，真实物品会发入玩家背包，奖励命令由控制台执行。背包放不下的物品会保留在待领取列表。

领取待发奖励：

```text
/rc claim
```

插件会在开箱结束、玩家退出、死亡、传送、插件关闭或异常中断时恢复玩家原位置、游戏模式、镜头状态和临时模型实体，并保留未安全发放的奖励。

## 10. 权限

| 权限 | 用途 | 默认 |
| --- | --- | --- |
| `rookiecrates.use` | 使用玩家 GUI 和右键宝箱 | 所有玩家 |
| `rookiecrates.admin` | 管理宝箱、奖励、点位、钥匙和导入导出 | OP |
| `rookiecrates.open.*` | 打开任意宝箱 | 所有玩家 |
| `rookiecrates.open.<宝箱ID>` | 打开指定宝箱 | 由权限插件配置 |

如果移除玩家的 `rookiecrates.open.*`，则需要按宝箱授予 `rookiecrates.open.<宝箱ID>`。

## 11. 全局配置、SQLite 和备份

默认数据库文件：

```text
plugins/RookieCrates/rookiecrates.db
```

`config.yml` 中可以调整：

- SQLite 文件名和忙等待时间。
- 旧版场景点的一次性迁移。
- 奖励揭示延迟、允许跳过时间和展示持续时间。
- 广播格式。
- GUI 标题和消息文本。

管理 GUI 中的“导出全部配置”会写入：

```text
plugins/RookieCrates/exports/rookiecrates-export.json
```

导出内容包括宝箱、奖池、场景、模型入口和玩家计数数据。导入功能读取同一路径的文件，并按 ID 合并或更新数据；导入前需要在聊天栏输入 `IMPORT` 确认，而且必须等待当前所有玩家开箱和领取操作结束。

## 12. 推荐的实际配置顺序

```text
安装 ModelEngine、RookieCrates 和可选的 Vault
→ 部署五个 bbmodel 并重启服务器
→ /rc admin 创建宝箱
→ 设置钥匙模板、价格、保底、模型和动画
→ 添加并检查奖池奖励
→ 设置 CRATE、CAMERA、LOOT_1..LOOT_7
→ 放置大厅右键模型入口
→ 检查控制台没有模型或动画错误
→ 启用宝箱
→ 发放测试钥匙
→ 分别测试单抽、七连抽、跳过和 /rc claim
→ 导出一份完整配置备份
```
