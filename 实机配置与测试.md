# RookieCrates 实机配置与测试手册

本文用于在独立 Paper 测试服中完成 RookieCrates 的首次部署、宝箱配置和功能验收。

## 1. 测试环境

需要准备：

- Java 21
- Paper 1.21.1
- ModelEngine R4.1.0
- RookieCrates
- Vault 与经济插件（仅当宝箱价格大于 `0` 时需要）

推荐将 `target/RookieCrates-1.0-SNAPSHOT-server-package.zip` 解压到服务器根目录。部署包包含：

```text
plugins/RookieCrates.jar
plugins/RookieCrates/config.yml
plugins/ModelEngine/blueprints/default_crate.bbmodel
plugins/ModelEngine/blueprints/loot_white.bbmodel
plugins/ModelEngine/blueprints/loot_blue.bbmodel
plugins/ModelEngine/blueprints/loot_pink.bbmodel
plugins/ModelEngine/blueprints/loot_yellow.bbmodel
```

ModelEngine 插件本体需要单独安装。首次测试使用全新数据库时，应确保服务器启动前不存在：

```text
plugins/RookieCrates/rookiecrates.db
```

完整启动服务器，并让测试客户端正确加载 ModelEngine 资源包。不要使用 `/reload`；替换插件或模型后应完整重启服务器。

## 2. 创建测试宝箱

使用拥有 `rookiecrates.admin` 权限的玩家执行：

```text
/rc admin
```

点击“创建宝箱”，输入：

```text
宝箱 ID：test
显示名称：测试宝箱
```

新建宝箱默认保持禁用，应在所有配置和测试准备完成后再启用。

## 3. 配置基础参数

进入 `test` 宝箱的管理页面：

1. 主手拿着钥匙物品，点击“实体钥匙模板”。
2. 将单抽价格和七连价格暂时设为 `0`。
3. 为快速测试，可暂时将 A 保底设为 `2`、S 保底设为 `4`。
4. 模型与动画输入：

   ```text
   default_crate|idle|open1|open7
   ```

   四项依次为宝箱模型、待机动画、单抽动画和七连动画。

Loot 模型由奖励等级自动选择：

| 等级 | 模型 | 颜色 |
| --- | --- | --- |
| C | `loot_white` | 白色 |
| B | `loot_blue` | 蓝色 |
| A | `loot_pink` | 粉色 |
| S | `loot_yellow` | 黄色 |

## 4. 创建测试奖池

建议每个等级至少创建一个物品奖励：

| 示例奖励 | 等级 | 测试权重 |
| --- | --- | ---: |
| 石头 | C | 70 |
| 铁锭 | B | 20 |
| 钻石 | A | 9 |
| 下界之星 | S | 1 |

创建物品奖励：

1. 将真实奖励物品拿在主手。
2. 点击“添加奖励”。
3. 输入奖励 ID 和显示名称。
4. 在奖励编辑 GUI 中点击“物品内容”。
5. 点击玩家背包中的物品，最多添加 27 组；左键上方物品移除，右键只取回副本。
6. 点击“保存并返回”，再设置权重、展示缩放、等级和广播开关。

创建命令奖励时保持主手为空，然后输入控制台命令，例如：

```text
give %player% emerald 3
```

一个奖品中的所有物品都会真殞进入玩家背包；第一个物品用于 Loot 模型展示。命令奖励由控制台执行。测试 S 级奖励时可以开启独立广播。

## 5. 配置场景点位

执行：

```text
/rc config test
```

需要配置完整的九个点位：

```text
CRATE
CAMERA
LOOT_1
LOOT_2
LOOT_3
LOOT_4
LOOT_5
LOOT_6
LOOT_7
```

点位 GUI 操作：

- 左键：保存玩家当前位置、世界、Yaw 和 Pitch。
- 右键：传送到已保存点位。
- `Shift + 左键`：进入精细编辑；未配置的点位会先使用玩家当前位置初始化。

精调步长：

- XYZ 普通点击：`0.01` 格。
- XYZ Shift 点击：`0.1` 格。
- Yaw/Pitch 普通点击：`1°`。
- Yaw/Pitch Shift 点击：`10°`。

建议先设置演出宝箱位置 `CRATE` 和镜头位置 `CAMERA`，再围绕宝箱依次设置 `LOOT_1..LOOT_7`。精调修改会立即保存到 SQLite，并将玩家传送到调整后的位置进行预览。

原有命令入口仍可作为备用：

```text
/rc scene test CRATE
/rc scene test CAMERA
/rc scene test LOOT_1
```

## 6. 随机预览七个 Loot

完整设置 `LOOT_1..LOOT_7` 后，执行：

```text
/rc preview test
```

也可以点击点位 GUI 中的“随机预览七个 Loot”。预览功能会：

- 根据已启用奖池的权重随机选择七个奖励。
- 在 `LOOT_1..LOOT_7` 生成对应物品和等级配色模型。
- 只向执行预览的管理员显示模型。
- 不消耗钥匙、金币或保底次数。
- 不发放物品、不执行奖励命令、不触发广播。
- 再次执行时替换上一次预览。
- 玩家退出、插件关闭或达到保留时间时自动清理。

默认保留时间为 30 秒，可在 `config.yml` 修改：

```yaml
preview:
  duration-ticks: 600
```

## 7. 放置正式右键入口

站到大厅中准备放置公共宝箱的位置，执行：

```text
/rc place test
```

也可以在宝箱管理 GUI 中左键点击“模型放置”。该入口是玩家右键打开宝箱的正式入口，与演出中的 `CRATE` 点位相互独立。

移除入口：

```text
/rc remove test
```

如果模型入口没有出现，应检查：

- ModelEngine 和资源包是否正常加载。
- `default_crate` 是否存在 `idle` 动画。
- `CRATE`、`CAMERA` 和 `LOOT_1..LOOT_7` 是否完整。
- 控制台是否报告未知模型、动画或世界未加载。

如果旧版 RookieCrates 报出 `ModelEngine rejected model: default_crate` 或
`ModelEngine rejected model: loot_white`，请替换为本次重新构建的
`plugins/RookieCrates.jar` 并完整重启。ModelEngine R4.1.0 首次成功挂载模型时会返回空的
“被替换模型”，旧版插件错误地将该返回值解释成拒绝；模型文件本身不需要因此改名。
同时确认 `plugins` 中不存在另一份旧的 RookieCrates 或 ModelEngine JAR，避免服务端加载到旧版本。

## 8. 启用并发放钥匙

完成钥匙、奖池、场景点和入口配置后，在管理 GUI 中启用宝箱。

发放实体钥匙：

```text
/rc key give <玩家名> test 14 physical
```

发放虚拟钥匙：

```text
/rc key give <玩家名> test 14 virtual
```

目标玩家必须在线。实体钥匙需要与管理 GUI 中保存的钥匙模板一致。

## 9. 功能验收

### 基础入口

- 玩家右键公共宝箱模型能够打开正式 GUI。
- 玩家将 `test` 的实体钥匙拿在主手右键空气或方块，也能直接打开 `test` 的正式 GUI；此时不会立即扣除钥匙。
- `/rc open test` 也能打开同一个宝箱。
- 未启用宝箱或没有权限时会被正确拒绝。

### 单抽

- 准确扣除 1 把钥匙。
- 金币价格大于 `0` 时准确扣款。
- 播放宝箱 `open1` 动画并显示一个 Loot。
- 抽中的真实物品进入玩家背包。
- 命令奖励只执行一次。

### 七连抽

- 准确扣除 7 把钥匙和七连套餐价格。
- 播放宝箱 `open7` 动画。
- 一次演出同时使用 `LOOT_1..LOOT_7`。
- 七个结果分别按照自身等级使用白、蓝、粉、黄模型。
- 七份奖励全部发放且没有重复执行命令。

### 保底、广播与跳过

- 使用临时的 A=2、S=4 阈值确认保底能够触发。
- S 级或单独开启广播的奖励能够正确广播。
- 达到 `opening.skip-after-ticks` 后，按 Shift 能跳过演出。
- 正式上线前恢复计划使用的保底阈值。

### 背包已满

1. 填满测试玩家背包。
2. 执行一次单抽或七连抽。
3. 确认无法放入的物品进入待领取记录。
4. 清出背包空间后执行：

   ```text
   /rc claim
   ```

5. 确认剩余奖励准确进入背包且不会重复领取。

### 场景恢复与清理

- 正常结束后恢复玩家原位置、游戏模式、飞行、碰撞和镜头。
- 跳过演出后仍能完整恢复。
- 演出中退出再重新进入时，恢复数据能够正确处理。
- 临时宝箱模型、七个 Loot 模型和镜头实体全部清理。
- 插件关闭时随机 Loot 预览不会留下假实体。

## 10. 上线前检查

- 恢复正式单抽和七连价格。
- 恢复正式 A/S 保底阈值。
- 检查所有奖励权重、等级、物品组数、每组数量和命令。
- 确认所有模型 ID、动画和 `ih_item` 持物骨骼有效。
- 分别使用方块和普通物品检查 Loot 大小；在各奖品编辑 GUI 的“展示缩放”中单独调整，然后再次预览。`loot-display.default-item-scale`（默认 `0.6`）只影响之后新建的奖品。
- 重新测试单抽、七连抽、满背包和 `/rc claim`。
- 在管理 GUI 中导出一份配置备份。
- 检查控制台无 SQLite、ModelEngine、Vault 或场景恢复错误。

完整配置说明参见 [`配置.md`](配置.md)，模型文件说明参见 [`model-assets/README.md`](model-assets/README.md)。
